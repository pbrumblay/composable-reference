# Fictional CMS for product taglines (e.g. mock server on port 7999)
CMS_URL=http://localhost:7999
CMS_API_KEY=your-cms-api-key

# Shared secret for internal API routes (load-catalog, invalidate-cache)
INTERNAL_API_KEY=your-internal-api-key

# Harper deploy (prebuilt tarball) — for npm run deploy:prebuilt
# HARPER_OPERATIONS_URL=https://your-instance:9925
# HARPER_USERNAME=HDB_ADMIN
# HARPER_PASSWORD=your-password